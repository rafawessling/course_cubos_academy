const ticket = true;
const censure = 16;
const age = 18;

if (ticket === true && age >= censure) {
    console.log('Allowed to watch the movie.');
} else {
    console.log('Not allowed to watch the movie.');
}
