const namesStudents = ['jose', 'joao', 'maria', 'joana', 'pedro'];

for (let name of namesStudents) {
    console.log(name);
}

/*for (let index = 0; index < namesStudents.length; index++) {
    console.log(namesStudents[index]);
}*/
